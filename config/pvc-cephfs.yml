apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ocs-storagecluster-cephfs
  resources:
    requests:
      storage: 1Gi

