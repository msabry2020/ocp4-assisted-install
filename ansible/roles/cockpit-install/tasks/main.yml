- name: Install Cockpit
  package:
    name: 
      - cockpit
      - cockpit-machines
    state: present

- name: Start and enable Cockpit
  systemd:
    name: cockpit.socket
    state: started
    enabled: yes
